<%= simple_form_for @city do |f| %>
  <%= f.input :name %>
  <%= f.input :state_id, collection: State.all %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :landmark %>

  <%= f.button :submit %>
<% end %>