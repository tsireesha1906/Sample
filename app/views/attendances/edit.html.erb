<%= simple_form_for @attendance do |f| %>
  <%= f.input :student_id, collection: Student.all %>
  <%= f.input :attendance_status, collection: ["Present", "Absent"] %>

  <%= f.button :submit %>
<% end %>
